<script>
console.log("hiii")
  $(document).ready( function() {
    $(".status").on("ajax:success", ".like-button", function(event, data) {
      var $button = $(event.target)
      var $likes = $button.parent(".likes")
      var $html = data

      console.log(data)
      console.log($likes)

      $button.hide();
      $likes.html($html)
    });

    $(".status").on("ajax:success", ".comment-form", function(event, data) {
      var $form = $(event.target)
      var $commentList = $form.siblings(".comment-list")
      var $html = data

      $form[0].reset();
      $commentList.append($html)
    });
  })
</script>