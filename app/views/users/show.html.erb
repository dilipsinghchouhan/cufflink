<% if current_user == @user %>
  <%= render partial: "shared/show-router", locals: { type: "user",
    object: @user, edit: true } %>
<% else %>
  <%= render partial: "shared/show-router", locals: { type: "user", object:
    @user, edit: false } %>
<% end %>

