<% if current_user == @user %>
  <%= render partial: "shared/show-and-edit", locals: { type: "user", object: @user } %>
<% else %>
  <%= render partial: "shared/show", locals: { type: "user", object: @user } %>
<% end %>