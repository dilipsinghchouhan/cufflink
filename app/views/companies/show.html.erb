<% if current_user.managed_companies.include?(@company) %>
  <%= render partial: "shared/show-router", locals: { type: "company",
    object: @company, edit: true } %>
<% else %>
  <%= render partial: "shared/show-router", locals: { type: "company", object:
    @company, edit: false } %>
<% end %>
